.venv/
__pycache__/
*.pyc
.pytest_cache/
*.tmp
data/build.log

# Daily source snapshots: ~3 MB/day and refetchable. CI carries these in the
# Actions cache; data/cache/wayback stays tracked because it cannot be refetched cheaply.
data/raw/
